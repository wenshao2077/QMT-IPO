# 3.2：运行记录与午间查询

本次优化不改变Owner现有授权、计划任务开关、提交时段、数量算法或去重保护，不触发补单。

## 现在能看到什么

默认打开“运行记录”页，显示今日每次自动申购、通知补发、监控和备份触发：开始时间、实际结果、本轮查询/提交数、耗时。
双击一行可查看结束时间、查询步骤、项目结果、跳过原因和下一申报/复核节点。
顶部同时显示最近一次自动申购触发结果，并标注数据读取时间，默认每10秒刷新。
原“操作记录”改名为“授权/暂停记录”，与运行记录分开。旧回执也可阅读；历史字段缺失显示“未统计”，不会补造成功。

## 午间行为

11:30—13:00不再在查询前直接退出：可以连接账户、查询申购项目、校验元数据与额度、核对已有委托。
合法项目显示“已查询，等待13:00”，但不登记新的提交意图、不提交委托；13:00后按原任务周期继续处理。
午间查询不把当天标成最终完成，避免消耗下午的复核机会。
正常等待13:00不会被监控误判成申购失败；真实数据错误、结果不确定、超过等待节点仍未完成，继续告警。

## 北交所

Owner已确认无北交所权限；`.BJ`记录明确显示“北交所无权限，已跳过”。这不是系统错误，不会影响其他合法沪深项目。
显示查询返回1条、北交所跳过1条、提交0笔，比只显示退出码0更准确。

## 时段边界

查询、券商接收预委托、交易所接受申购是不同阶段。
[上交所官方股票投资办理说明](https://one.sse.com.cn/onething/gptz/)明确列出沪市网上申购时间为09:30—11:30、13:00—15:00；本次没有将提交延长到15:30，也没有放开午间提交。
程序既有的09:35开始与14:50停止新提交设置保持不变，本次只优化查询和可观测性。

## 部署与数据

新回执增加真实运行耗时和本轮activity字段；旧回执不改写。
配置文件、授权记录、去重SQLite和任务定义保持原样；代码替换前备份到小黑runtime/code-backups。
所有验证采用模拟券商或独立只读QA账本；没有手工触发实盘任务。

## 验收结果

- 本机64项测试通过；小黑SSH会话63项通过、1项可见窗口布局跳过，真实桌面已独立验证运行表格可见。
- 更新后控制台能读取并显示今日此前的28条任务记录，旧版“等待13:00、未查询”的回执没有被改写。
- 12:32独立只读QA完成午间查询：返回1条北交所记录、跳过1条、提交0笔；QA消息未发送。
- 12:35:01—12:35:04，Owner已启用的计划自然触发3.2版本，耗时2.72秒：连接与账户正常，查询返回1条920229.BJ，原因明确为“北交所无权限，已跳过”，提交0笔，下一复核节点13:00。
- 原授权保持true，任务开关未改变；本轮没有启动、重启或补发实盘申购任务。
