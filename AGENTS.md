# AGENTS — QMT打新交付与部署边界

适用于仓库全部文件。本版为 3.3.0-rc1，基线 `922f7b1`；README 与本文件优先于历史验收文档。历史“小黑验证通过”不能移作本轮结论。

## 不可越过的边界

网页开发/CI 只做代码、文档、隔离测试、分支/PR。不得读取真实账号/Webhook，不连接真实账户、不发送真实消息、不执行 live、不启用或手动触发生产任务、不部署小黑、不重启 QMT。没有卖出、缴款、资金划转。不要添加服务端、远程交易代理或额外平台依赖。

只读账户探针也需要本机明确授权。`diagnostics check` 和 `calendar status` 是不联网的配置/本地日历检查；`calendar refresh` 是可能联系 miniQMT 数据端/网络的维护命令；`probe_readonly` 连接真实账户；`test-webhook` 是真实发送。不能混淆这四类权限。

## 修改代码前后

- 先读 `coordinator.py`、`support.py`、`mini_base.py`、`app.py` 及相关测试。保留原申报窗口、数量/价格/市场算法；单独说明任何明确缺陷修复。
- 唯一允许新增请求的路径仍为：有效交易日 → 合法窗口/账户就绪 → 核对现存委托与原账本 → durable reserve → submit。结果不确定、已存在意图、回报异常均不得重报。
- shared calendar 的 decide 必须纯本地无账户连接；unknown 不得提交。旧缓存必须具有可靠覆盖依据；年度公告包不得外推下一年；周末不使用调休工作日规则。
- 不自动删除/新建丢失的生产 ledger，不擅自改 state_dir。新增的 notification_suppressions 是可审计侧表，不得 set delivered=1 冒充发送。
- 更新源码后执行 `python -B build_manifest.py`，清单不包含私有配置或运行文件。清单只证明文件一致性，不证明上游来源可信。
- 执行四个 unittest 模块；Linux 用 Xvfb，Windows 用已登录桌面或离线 CI；PowerShell 只运行 test_tasks.ps1 的定义/解析检查，不注册任务。
- 代码提交在 review 分支；不得自动 merge main 或创建代表生产验收的 release。报告实际执行环境、命令、计数、失败和跳过；未执行的测试不得称通过。

## 本机 TARS 升级清单

首先确认 Owner 批准部署操作；本轮网页修改不是部署授权。核对所审查提交、交付清单；在单独源码目录准备包；保存现有配置哈希、真实 state_dir、账本一致性备份和四个任务定义/开关。配置、消息密钥、数据库与生产日志只能留本机受保护目录。

使用 `install.ps1 -Mode Upgrade -Root <原根目录>`，不要 New，不要重装依赖，不要逐文件热拷贝。包装器精确检验四个任务的现代/3.2 入口和工作目录，保存原状态、暂时禁止触发、确认无运行中的 worker/GUI 后，更新器才持有安装/申购/监控锁替换清单内源码。成功恢复原开关；不会主动启动任务。维护失败标记不得随手删掉。

新安装与升级分开：只有 New 可以初始化账本，默认关闭执行。原账号、Webhook 文件、state_dir、daily/history、授权和任务开关在 Upgrade 中必须保持。新版本首次运行可幂等创建非交易性的通知抑制侧表，但不改写 intents/outbox 历史。

完成后：只读核验周末跳过、工作日日历及覆盖、2027未知、真实 SDK 的日期格式和覆盖、账户连接权限、中文/空格路径、普通用户任务权限、时区 +08:00、无窗口启动、配置/账本身份和所有开关。不因一个单元测试或探针成功就宣布真实申购已验收；真实受理仅依据获准后原计划自然触发的券商回报。

## 回滚与事故

用保留的新源码包 `install.ps1 -Mode Rollback ... -RollbackId ...`，同样先静止任务和GUI。只恢复代码，不恢复旧账本；任何升级后已有的意图、券商回报都必须保留。回到3.2会重新出现休市连接误报，旧版忽略通知抑制表；提前决定是否保持暂停并明确告知。

若无法取得锁、配置/账本校验失败、缺少任务、发现不属于此根目录的动作或无法确认执行状态：停止，保存诊断摘要，人工核验。不得猜路径/重建账本/杀 QMT/补报订单来“修复”。

## QuantClass

period_offset/offset 只可能是后续可选只读适配。当前未获得客户端/数据库，不能声称检查过数据，不能猜表名或字段。接入前记录真实结构、抽样日期、覆盖首尾、完整性与 offset 的日期语义，再与交易所及QMT日历比对；缺少证明就保持禁用。该适配不得成为首次安装的必需条件。
