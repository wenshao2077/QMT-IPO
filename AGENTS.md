# AGENTS — 3.4.0 最终交付与部署边界

适用于整个源码包。版本 3.4.0，基线 `bd667bf8dd072e8467b91432f1eb9986bce43e15`。优先读 docs/FINAL_DELIVERY.md、docs/ROUND3.md、README、AI_DEPLOY、DEPLOY_PROTOCOL 和本轮实际测试记录；历史通过不能移用为本轮结论。

## 部署代理

只调用统一 `setup.ps1` 入口以及其中声明的操作。先 VerifyPackage → Plan，再由用户批准 New/ResumeNew/Upgrade 等对应写操作；Doctor 不连接账户。读取 local_log 是本机排障，不得自动上传。

账户和 Webhook 由用户在本机 Configure 向导填写，不通过对话、命令行参数、环境变量回显或共享文件传递。只读取脱敏 JSON。没有读取真实密钥、数据库、客户端用户数据或生产日志的默认授权。

安装与首次配置默认保持交易关闭。升级/回滚保留原开关，不能把暂停的实例开启，也不能主动触发任务。`Configure` 关闭窗口不证明已保存，以后续 Doctor 和用户在本机确认的结果为准。

不要因为安装成功就连接真实账户。账户只读探针、行情日历联网更新、真实测试通知、每日交易启用是四种不同权限。普通聊天 AI 没有本机执行能力时不得冒充已部署；本机代理也不能自行扩大授权。

禁止为修复而猜账户/账本路径、覆盖 config.json、删库重建、清除不确定意图、删除维护/配置中断标记、强杀 worker/QMT、重启客户端、手动补报、代替用户启用交易。没有账户变更、自动卖出、缴款、资金划转、远程代理或云端交易能力。

## 开发代理与 CI

只修改代码/文档、隔离测试、审查分支。不得部署生产、访问真实账户/密钥、发送真实通知、调用 live 对接真实 broker、启用/触发生产任务、合并 main 或发布代表生产验收的 Release。

必须保留：有效交易日 → 原窗口和账户就绪 → 核对既有委托和账本 → 先持久化意图 → 最多首次提交。不确定/已有意图只核对，不重发。休市和未知不连接交易账户，不把周末调休当交易日，不外推年度日历。

第一阶段的业务差异是有限时点再查询；本轮不修改该规则，不改变市场集合、申报时段、价格、数量、最小单位、remark 或持久化去重。已完成只代表截至本轮，15:05 只核对。

源码改动后重建 DELIVERY_MANIFEST。提交前执行原四组测试及 test_delivery_round1、test_discovery_round1、test_release_round1、test_maintenance_round2、test_package_round2、test_ui_round2、test_final_delivery。Linux GUI 使用 Xvfb，Windows 使用可见桌面或离线 runner。报告环境、测试数、失败/跳过及未验证项。不得称 Linux 模拟检查为 Windows 验收通过。

源码包中的新建/升级/回滚工具只服务受控本机流程。回滚不能恢复旧委托数据库。安装中断无法证实完整状态时，保留现场并停止；日志和恢复标记不能用成功状态覆盖。

## 其他集成

不新增 QuantClass 等平台依赖；没有读取其真实数据，不猜表结构。SDK/client 的实际兼容性及券商受理需要另行授权和证据。本源码候选没有厂商二进制再分发授权或正式发行签名，不得声称已获得。

## 第二阶段维护边界

以用户已验收的alpha1 ZIP为代码基线，不把GitHub旧main当最新交付。Health、RecoveryPlan和CalendarCoverage只读；ExportSupport只按明确的输出路径和确认创建本地脱敏ZIP，绝不上传。RecoveryPlan不是执行修复的授权。

通知新旁路表只记录新发送成功的确认时间；旧delivered/抑制记录不补造时间，回滚不删旁路审计。未知通知状态不能压成0。日历前瞻仍纯本地，未知不猜测。不要用immutable=1忽略活动WAL，不手工删除SQLite辅助文件。

包构建后必须对最终ZIP进行结构/哈希核验和解压重测。完整性不是签名；直接组件清单不是完整SBOM。SQLite运行时告警不授权自动替换运行库或SDK。SOURCE_FILES是人工审查的分发白名单，添加交付文件必须显式更新并重建清单。
