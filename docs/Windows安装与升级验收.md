# Windows 安装与升级验收

日期：2026-09-12。版本：3.3.0-rc1，本机修订。Owner 已授权修正测试、安装与升级验收、同步 GitHub。

## 修正内容

- 路径测试改为比较实际 argv 和 cwd，不再比较 Mock 的转义调试字符串。
- Windows PowerShell 5.1 通过 `-File` 启动时，参数默认值中的 `$PSScriptRoot` 未能提供源码目录；将默认值初始化移到脚本主体。增加真实 `powershell.exe -File` 拒绝同源同目标的无副作用回归检查。
- 申购数量、价格、范围、时段及持久化去重逻辑未改变。

## 已执行

| 项目 | 结果 |
|---|---|
| 主力机 Windows / Python 3.11 | 109 项通过，0 失败、0 跳过 |
| Windows PowerShell 5.1 / PowerShell 7 | 四个禁用任务定义、时区、路径归属及 10 个脚本解析通过 |
| 全新安装 | 中文和空格目录、独立环境及固定依赖安装成功；默认假账号、空 Webhook、执行关闭、四个任务禁用 |
| 调度入口 | 隔离假账号安装的关闭执行入口由任务调度器实际启动，退出码 0；不创建交易 worker、不连接或发送 |
| 更新器实跑 | 隔离安装升级成功，原关闭状态保留 |
| 目标 Windows worker | 109 项运行，108 通过、1 项因 SSH 无可见桌面跳过；真实桌面另行验证加载 |
| 3.2 → 3.3 升级 | 使用正式 Upgrade 包装器完成；正常关闭旧控制台，无强杀、无客户端重启 |
| 私有状态 | 配置、Webhook、原账本文件 SHA-256 及 intents/outbox 行摘要前后一致；state_dir 与执行开关保留 |
| 计划与界面 | 四个原任务均恢复 Enabled/Ready；原控制台入口重新打开，状态 enabled，needs_admin=false，未调用交易按钮 |
| 日历 | 2026-09-12 休市、09-14 交易、10-01 休市、2027-01-04 未知；隔离休市流程零连接、零提交、零通知 |
| 配置检查 | 生产安装 diagnostics check 返回 ok=true、issues=[]、message_sent=false |

真实私有备份和维护回执只保存在目标 worker，不上传账号、密钥或数据库。升级回滚只恢复代码，不恢复旧账本。此次未执行生产回滚；其业务语义由隔离测试覆盖。

## 验收边界

当前是周末，miniQMT 没有运行；未启动或重启客户端，因此本轮没有重新验收真实账户连接、SDK日历返回值或券商申购受理。未发送真实测试通知、未手动触发生产申购任务；原已授权计划继续按原开关运行。

日历内置包仅覆盖 2026 年。后续年度公告需维护，不能将“未知”自动变成交易日。

网页端 Linux 历史测试保留在 TEST_RESULTS.md。本记录补充本轮 Windows 证据，不把离线测试或安装成功当作真实申购验收。代码同步至审查分支，不自动合并 main 或发布正式 Release。
