> 历史版本记录；个人路径已改为逻辑占位符。当前候选版部署与边界以 README.md / AGENTS.md 为准，不以历史验收代替本轮验收。

# 每日自动申购 V3

> 当前交互已升级为[桌面控制台3.1](桌面控制台使用说明.md)。使用桌面的“自动打新控制台”，通过按钮操作；下文旧脚本快捷方式已归入“旧版打新入口（保留）”。申购引擎和日程未因此改变。

## 目标与操作方式

本版按“每天开盘后自动完成新股、新债申购”设计，不以论坛附件的程序生命周期为产品边界。
股票申购取账户对应市场额度与发行上限的较小值；新债按发行上限申购，不占用股票市值额度，数量不超过官方接口定义的10000张。
只处理申购，不提供卖出、缴款、资金划转、撤单或QMT/客户端自动重启。

正常使用时，Owner只需一次性启用Windows计划任务。之后无需每天点击程序或输入确认词。
每轮工作完成后进程退出，Windows负责下一次唤起；“没有常驻窗口”不等于自动化没运行。

## 四个任务

| 任务 | 调度与职责 |
|---|---|
| QmtIPO3-Cycle | 每天09:35开始，每5分钟处理到14:50；午休不提交；15:05最终只读核对；用户登录时补一次检查 |
| QmtIPO3-Notify | 09:37起每5分钟补发通知；不提交申购 |
| QmtIPO3-Monitor | 09:26起每5分钟检查任务回执、当日进度、日历和通知积压；独立告警队列 |
| QmtIPO3-Backup | 16:20做SQLite一致性备份，保留去重记录 |

任务使用当前Windows用户的InteractiveToken、无窗口包装、IgnoreNew、StartWhenAvailable和WakeToRun。
QMT仍需处于已登录状态。开盘前运行只记录等待09:35的计划，不会要求用户开盘后再手工点一次；到时由已启用的计划任务触发。
如果机器晚开或QMT暂未登录，后续触发继续处理未完成项目；当天已经完成则跳过，只保留15:05最终核对。
每个工作子进程最多180秒，包装器只结束自己创建的超时子进程，绝不按进程名结束QMT/其他Python。

## 为什么不会重复申购

- 升级继续使用V2的同一份SQLite和同一把实例锁，不清空原账本。
- 每只证券以账户摘要、申购日期和代码为唯一键；真正发请求前先持久保存意图。
- 进程被终止、网络超时或返回值不确定之后，只查询券商状态，不直接再次提交。
- 券商已有人工委托也会跳过；已有废单、已撤或不确定委托转为需核对，不能由“失败重试”绕过去重保护。
- 一个新股额度查询失败，不会阻止独立的新债项目；下一轮只补尚未发起过请求的项目。
- 同一市场的新股额度可在多只不同新股间复用，不把市值额度当现金逐只扣减。

本版保证的是共用同一完整账本的程序不重复发起同日同代码请求；无法把人工/其他系统在最后一次查询之后的并发操作也变成全局原子事务。
不可删除去重数据库来“重新开始”。

## 可观测结果

每天有独立的live/preview状态文件，预览成功不能成为实盘完成凭据。
每次运行保存开始和结束回执，即使崩溃也能发现“开始后未结束”。
结果汇总到每只证券：已报、已成、已有委托跳过、额度不足、市场未启用、数据待重试、结果不确定、废单或窗口已错过。
已报/已成不表示中签；中签确认与资金处理不在本版范围。

查询空列表不能一次就判定当天无申购：需两次、间隔至少两分钟的空查询确认。
部分项目已有记录后，后续空列表不会抹掉原项目或把失败变成完成。
临时连接/查询异常按30分钟窗口限频；通知只在企微HTTP200且整数errcode=0时标为送达，失败保留队列并退避补发。
消息UTF-8分页不超过2000字节。网络响应丢失可能造成同事件编号消息再次发送，但不会引发重新申购。

## 对23893和现行接口的复核

- [23893原帖](https://bbs.quantclass.cn/thread/23893)：2023-04-27发布，当前正文的关键是把BOND分支加入09:35的TaskControl。公开展示依赖当时的Rocket ExchangeAPI，并包含QMT重启循环。2026-09-09重新读取正文与当前1条评论；未见新的适配说明。精华和奖励说明帖子获得认可，不能代替当前券商接口验收。
- [迅投现行原生交易API](https://dict.thinktrader.net/nativeApi/xttrader.html)：页面显示最后更新2026-05-08；仍定义query_ipo_data、query_new_purchase_limit、order_stock、query_stock_orders、query_account_status及订单错误回调。
- 现行文档明确：IPO类型STOCK/BOND，股票单位为股、债券为张；股票额度分SH/SZ/KCB，债券申购额度固定10000张。本版不套用三年前的固定股票单位，而消费当日minPurchaseNum/maxPurchaseNum。
- 小黑现役SDK250807.1.2及账户只读查询已实测可用；不因为帖子较老而替换整个SDK，也不把Rocket内部旧接口移植进现役客户端。
- 原帖中自动重启部分不采用：小黑重启职责继续归彩虹守护，申购应用只对自己的连接做重连和查询。

本次是工程重构，不是策略回测复现；正文/当前评论用于设计取证，没有声称完整附件归档或历史实盘业绩复现。

## 今日真实接口样本

2026-09-09只读复核时：账户状态正常，IPO接口返回1条`920229.BJ`，`purchaseDate='0'`、单位100、上限467500。
它既不是沪深证券，也没有有效申购日期，不能据此声称今天有可执行的沪深新股/新债申购。
当前部署范围继承既有SH/SZ设置；代码支持经配置启用KCB，不支持BJ。
对这条真实异常样本已补测试：跳过范围外项目，不会阻止同时出现的合法沪深新股/新债。
不把只读连接和字段校验通过夸大为券商真实受理通过。

## 小黑位置与启用

根目录：`<原安装根目录>/`。
代码在`code/`；独立`.venv`；配置为`config.json`；V3进度在`runtime/`。
去重和通知数据仍位于`<原配置state_dir>/state.sqlite3`，保留跨版本保护；Webhook继续引用原私有文件，不随源码打包。

桌面两个入口：

1. **自动打新V3—启用每日计划**：由Owner本人运行并一次性确认后，将配置改为true，启用新任务，并停用旧五个QmtIPO只读任务。不会改变QuantPush、Rocket或彩虹守护。
2. **自动打新V3—查看运行状态**：查看实际开关、任务状态、下一次运行时间和当日处理明细。

TARS本次交付时，新任务为Disabled、配置为false，未执行启用入口、未提交委托、未切换现役任务。Owner完成一次启用后，程序才按上面的日程自动申购。
不需要每天再次执行旧的“实盘手动启动”快捷方式。旧入口暂保留作历史回退，不应同时作为日常工作入口。

## 验证与回退

测试使用完全模拟的券商和HTTP，禁止网络；覆盖跨交易日自动运行、早启动、晚开机、QMT掉线恢复、部分成功续跑、超时回执、进程恢复、旧账本复用、人工委托去重、预览/实盘隔离、空数据复核、盘后核对、通知和备份。
运行测试：`uv run --isolated --no-project --python 3.11 python -B -m unittest -v test_system`。

旧版代码、私有配置和五个已启用任务保留；启用脚本会备份旧任务XML和V3配置，再做切换。
停用V3只停用四个QmtIPO3任务，不删除账本；若已产生真实委托，不回滚请求记录，也不以恢复旧代码为理由重报。
既有NAS探针仍只检查小黑TCP连接性，不是跨机业务心跳；本次不扩大NAS权限或修改SSH访问通道。
