# 自动打新控制台 3.1

> 当前已升级3.2：[运行记录与午间查询说明](运行记录与午间查询说明.md)。默认页改为“运行记录”，授权/暂停记录单独保留；午间可以查询，但提交时段没有放宽。

## 现在只用一个入口

在小黑桌面双击 **自动打新控制台**。
这是本机图形窗口，不需要浏览器、域名或ChatGPT连接，不会弹出命令行要求输入确认词。
打开面板只读取状态，不会自动授权、启用任务或提交申购。

旧的四个脚本启动快捷方式已移至桌面 **旧版打新入口（保留）**，没有删除源代码、账号配置或去重账本。

## 按钮

| 按钮 | 作用 |
|---|---|
| 授权并启用每日打新 | 显示账户尾号和授权范围；本人点击“同意授权并启用”后，程序处理任务切换。无需每天再次确认 |
| 暂停每日打新 | 本人确认后关闭后续申购计划；已经开始的一轮可能完成，不撤销已经提交的委托。通知补发和备份仍可继续 |
| 检查QMT连接 | 只读检查客户端连接、账户状态和数据接口，不提交申购 |
| 打开日志目录 | 打开本机运行记录目录 |

关闭面板不停止已启用的每日计划。需要停止时使用“暂停每日打新”。
重复双击桌面入口会优先显示已有控制台，不重复打开多个窗口。

## 看什么

- 顶部状态由配置和实际计划任务共同判定，不能只凭一个true/false假装成功。
- “今日处理”展示当日实盘记录；没有记录显示“尚未开始”，不冒充“今日无新股”。
- “下一次触发”来自Windows实际计划任务；未启用时明确显示待启用。
- “通知队列”显示待发送消息数，读取失败不会显示为0。
- “今日明细”列出证券、数量和委托状态；券商已报/已成不代表中签。
- “计划与环境”显示账户尾号、市场范围、客户端位置及通知配置情况，不显示Webhook密钥。
- “操作记录”显示授权、暂停、权限准备的成功或失败，错误提示不会被下一次自动刷新立即抹去。

## 权限问题已处理

之前失败的原因是：管理员创建的任务只给普通用户读取权限，旧桌面入口却没有处理提权。
本次仅为当前Windows用户补齐这套程序的九个既有任务的管理权限，保留其他权限条目，不修改其他任务或Windows账户权限。
准备权限前后核对了实盘配置与任务启用状态，均未改变。

已在普通桌面会话实测：对一个无交易功能、无自动触发的测试任务成功启用再停用，is_admin=false；该测试没有改动真实申购任务。两个测试任务已清理，验收记录保留。
当前普通用户的控制台显示needs_admin=false，正常使用不需要右键“以管理员身份运行”。
以后若任务被其他工具重建导致权限丢失，程序会在按钮操作时自动提示Windows系统授权；取消授权不会继续启用申购。

## 本轮验证

- 本机55项测试通过：原38项业务流程加17项GUI/控制测试。
- 小黑SSH会话：54项通过，1项可见窗口布局测试因非交互会话跳过。
- 小黑真实普通桌面会话：控制台已加载，授权、暂停、连接检查和日志按钮均可见，表格高度181像素；显示缩放导致的底部按钮挤出问题已修正。
- 普通用户任务权限读写实测通过；界面加载未调用金融控制操作。
- 本轮没有点击真实授权/暂停按钮，没有提交委托，没有修改申购引擎代码，也没有启用真实申购任务。

## 位置

小黑主目录仍为 `C:/Users/77184/qmt-ipo-v3/`，没有另造一套交易环境。

- `code/panel.py`：界面。
- `code/panel_backend.py`：只读状态聚合、按钮调用和必要时的Windows授权引导。
- `panel_tasks.ps1`：限定九个任务的权限准备及用户按钮控制，带结果回读和操作记录。
- `runtime/panel-actions/`：控制操作回执、权限备份。
- `runtime/panel-permission-test.json`：普通用户权限实测结果。
- `runtime/panel-visible.json`：真实桌面加载/布局验证结果。
- `QmtIPO-Panel-Open`：仅用于手动打开控制台，无定时触发，不提交申购；日常桌面快捷方式直接打开窗口。

实际申购链路仍使用原V3代码、原共享去重账本、原通知凭据和原计划时刻。市场范围与日常处理规则见[需求与运行说明](需求与运行说明.md)。
